export type menuItemType = {
  id: string;
  name: string;
  anchor: string;
};

export type TitlePropsType = {
  title: string;
  uniqueName: string;
};

export type SubtitlePropsType = {
  subtitle: string;
};
